apiVersion: v1
kind: ConfigMap
metadata:
  name: pg-backups-disabled
  namespace: ${APP_CHAIN_INDEXER_NAMESPACE}
data:
  reason: PG_BACKUPS_ENABLED is false for this cluster, so no ObjectStore and no ScheduledBackup are created.
